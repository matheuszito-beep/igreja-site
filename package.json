{
  "name": "igreja-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site estático da igreja — pronto para hospedagem compartilhada (Hostinger).",
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test \"tests/**/*.test.js\""
  }
}
